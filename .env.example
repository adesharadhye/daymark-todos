# Listen on this HTTP port; the development Vite proxy expects 3001.
PORT=3001
# Bind to localhost so the app is reachable only from this machine by default.
HOST=127.0.0.1
# Persist tasks in this SQLite file; the server creates its parent directory.
DATABASE_PATH=./data/daymark.sqlite
